//Call the functions here?
//Tab switching logic here? For Display, (Today, This week, Completed) Event listeners - You should have event listeners for each tab
// that wipes out the current 
//contents and then runs the correct ‘tab module’ to populate it again.
//Each 'tab' will have its own module, Each module will export a function that creates a div element, 
//adds the appropriate content and styles to that element and then appends it to the DOM